/******************************************************************************
 *  Copyright (C) 2017 Broadcom. The term "Broadcom" refers to Broadcom Limited and/or its subsidiaries.
 *
 *  This program is the proprietary software of Broadcom and/or its licensors,
 *  and may only be used, duplicated, modified or distributed pursuant to the terms and
 *  conditions of a separate, written license agreement executed between you and Broadcom
 *  (an "Authorized License").  Except as set forth in an Authorized License, Broadcom grants
 *  no license (express or implied), right to use, or waiver of any kind with respect to the
 *  Software, and Broadcom expressly reserves all rights in and to the Software and all
 *  intellectual property rights therein.  IF YOU HAVE NO AUTHORIZED LICENSE, THEN YOU
 *  HAVE NO RIGHT TO USE THIS SOFTWARE IN ANY WAY, AND SHOULD IMMEDIATELY
 *  NOTIFY BROADCOM AND DISCONTINUE ALL USE OF THE SOFTWARE.
 *
 *  Except as expressly set forth in the Authorized License,
 *
 *  1.     This program, including its structure, sequence and organization, constitutes the valuable trade
 *  secrets of Broadcom, and you shall use all reasonable efforts to protect the confidentiality thereof,
 *  and to use this information only in connection with your use of Broadcom integrated circuit products.
 *
 *  2.     TO THE MAXIMUM EXTENT PERMITTED BY LAW, THE SOFTWARE IS PROVIDED "AS IS"
 *  AND WITH ALL FAULTS AND BROADCOM MAKES NO PROMISES, REPRESENTATIONS OR
 *  WARRANTIES, EITHER EXPRESS, IMPLIED, STATUTORY, OR OTHERWISE, WITH RESPECT TO
 *  THE SOFTWARE.  BROADCOM SPECIFICALLY DISCLAIMS ANY AND ALL IMPLIED WARRANTIES
 *  OF TITLE, MERCHANTABILITY, NONINFRINGEMENT, FITNESS FOR A PARTICULAR PURPOSE,
 *  LACK OF VIRUSES, ACCURACY OR COMPLETENESS, QUIET ENJOYMENT, QUIET POSSESSION
 *  OR CORRESPONDENCE TO DESCRIPTION. YOU ASSUME THE ENTIRE RISK ARISING OUT OF
 *  USE OR PERFORMANCE OF THE SOFTWARE.
 *
 *  3.     TO THE MAXIMUM EXTENT PERMITTED BY LAW, IN NO EVENT SHALL BROADCOM OR ITS
 *  LICENSORS BE LIABLE FOR (i) CONSEQUENTIAL, INCIDENTAL, SPECIAL, INDIRECT, OR
 *  EXEMPLARY DAMAGES WHATSOEVER ARISING OUT OF OR IN ANY WAY RELATING TO YOUR
 *  USE OF OR INABILITY TO USE THE SOFTWARE EVEN IF BROADCOM HAS BEEN ADVISED OF
 *  THE POSSIBILITY OF SUCH DAMAGES; OR (ii) ANY AMOUNT IN EXCESS OF THE AMOUNT
 *  ACTUALLY PAID FOR THE SOFTWARE ITSELF OR U.S. $1, WHICHEVER IS GREATER. THESE
 *  LIMITATIONS SHALL APPLY NOTWITHSTANDING ANY FAILURE OF ESSENTIAL PURPOSE OF
 *  ANY LIMITED REMEDY.
******************************************************************************/

/* This file is autogenerated, do not edit. */

#include "../bhsm_bsp_msg.h"
#include "bsp_components.h"
#include "bsp_keyladder.h"
#include "bhsm_p_keyladder.h"


BDBG_MODULE(BHSMa);


BERR_Code BHSM_P_KeyLadder_RouteKey( BHSM_Handle hHsm, BHSM_P_KeyLadderRouteKey *pParam )
{
	BERR_Code rc = BERR_SUCCESS;
	BHSM_BspMsg_h hMsg = NULL;
	uint16_t bspError = 0;
	BHSM_BspMsgCreate_t msgCreate;
	BHSM_BspMsgConfigure_t msgConfig;
	Bsp_KeyLadder_RouteKey_InFields_t *pSend = NULL;

	BDBG_ENTER( BHSM_P_KeyLadder_RouteKey );

	BKNI_Memset( &msgCreate, 0, sizeof(msgCreate) );
	BKNI_Memset( &msgConfig, 0, sizeof(msgConfig) );

	hMsg = BHSM_BspMsg_Create( hHsm, &msgCreate );
	if( !hMsg ) { return BERR_TRACE( BHSM_STATUS_RESOURCE_ALLOCATION_ERROR ); }

	pSend = (Bsp_KeyLadder_RouteKey_InFields_t*)msgCreate.pSend;

	msgConfig.component = Bsp_CmdComponent_eKeyLadder;
	msgConfig.command = Bsp_CmdKeyLadder_eRouteKey;

	rc = BHSM_BspMsg_Configure( hMsg, &msgConfig );
	if( rc != BERR_SUCCESS ) { return BERR_TRACE( rc ); }

	/* pack input paramters */
	pSend->blockType = pParam->in.blockType;
	pSend->entryType = pParam->in.entryType;
	pSend->keySlotType = pParam->in.keySlotType;
	pSend->keySlotNumber = pParam->in.keySlotNumber;
	pSend->sc01ModeWordMapping = pParam->in.sc01ModeWordMapping;
	pSend->gPipeSc01EntryType = pParam->in.gPipeSc01EntryType;
	pSend->rPipeSc01EntryType = pParam->in.rPipeSc01EntryType;
	pSend->keyMode = pParam->in.keyMode;
	BKNI_Memcpy( pSend->modeWords, pParam->in.modeWords, 16 );
	pSend->extIvPtr = pParam->in.extIvPtr;
	pSend->vklId = pParam->in.vklId;
	pSend->keyLayer = pParam->in.keyLayer;
	pSend->swapAesKey = pParam->in.swapAesKey;

	rc = BHSM_BspMsg_SubmitCommand ( hMsg, &bspError );
	if( rc != BERR_SUCCESS ) { BERR_TRACE( rc ); goto BHSM_P_DONE_LABEL; }

	if( bspError != 0 ) {
	    rc = BHSM_STATUS_BSP_ERROR;
	    BDBG_ERR(("%s BSP Status error [0x%X]", BSTD_FUNCTION, bspError ));
	    goto BHSM_P_DONE_LABEL;
	}


BHSM_P_DONE_LABEL:

	(void)BHSM_BspMsg_Destroy( hMsg );

	BDBG_LEAVE( BHSM_P_KeyLadder_RouteKey );

	return rc;
}


BERR_Code BHSM_P_KeyLadder_RootConfig( BHSM_Handle hHsm, BHSM_P_KeyLadderRootConfig *pParam )
{
	BERR_Code rc = BERR_SUCCESS;
	BHSM_BspMsg_h hMsg = NULL;
	uint16_t bspError = 0;
	BHSM_BspMsgCreate_t msgCreate;
	BHSM_BspMsgConfigure_t msgConfig;
	Bsp_KeyLadder_RootConfig_InFields_t *pSend = NULL;

	BDBG_ENTER( BHSM_P_KeyLadder_RootConfig );

	BKNI_Memset( &msgCreate, 0, sizeof(msgCreate) );
	BKNI_Memset( &msgConfig, 0, sizeof(msgConfig) );

	hMsg = BHSM_BspMsg_Create( hHsm, &msgCreate );
	if( !hMsg ) { return BERR_TRACE( BHSM_STATUS_RESOURCE_ALLOCATION_ERROR ); }

	pSend = (Bsp_KeyLadder_RootConfig_InFields_t*)msgCreate.pSend;

	msgConfig.component = Bsp_CmdComponent_eKeyLadder;
	msgConfig.command = Bsp_CmdKeyLadder_eRootConfig;

	rc = BHSM_BspMsg_Configure( hMsg, &msgConfig );
	if( rc != BERR_SUCCESS ) { return BERR_TRACE( rc ); }

	/* pack input paramters */
	pSend->vklId = pParam->in.vklId;
	pSend->askm3DesKlRootKeySwapEnable = pParam->in.askm3DesKlRootKeySwapEnable;
	pSend->keyLadderType = pParam->in.keyLadderType;
	pSend->rootKeySrc = pParam->in.rootKeySrc;
	pSend->customerSel = pParam->in.customerSel;
	pSend->askmSel = pParam->in.askmSel;
	pSend->kdfVersion = pParam->in.kdfVersion;
	pSend->keyLadderOperation = pParam->in.keyLadderOperation;
	pSend->globalKeyOwnerIdSelect = pParam->in.globalKeyOwnerIdSelect;
	pSend->globalKeyIndex = pParam->in.globalKeyIndex;
	pSend->keySize = pParam->in.keySize;
	BKNI_Memcpy( pSend->procIn, pParam->in.procIn, 32 );
	pSend->caVendorId = pParam->in.caVendorId;
	pSend->caVendorIdExtension = pParam->in.caVendorIdExtension;
	pSend->stbOwnerIdSel = pParam->in.stbOwnerIdSel;
	pSend->askmMaskKeySel = pParam->in.askmMaskKeySel;
	pSend->sageExclusiveModuleId = pParam->in.sageExclusiveModuleId;
	pSend->applyKkcv = pParam->in.applyKkcv;

	rc = BHSM_BspMsg_SubmitCommand ( hMsg, &bspError );
	if( rc != BERR_SUCCESS ) { BERR_TRACE( rc ); goto BHSM_P_DONE_LABEL; }

	if( bspError != 0 ) {
	    rc = BHSM_STATUS_BSP_ERROR;
	    BDBG_ERR(("%s BSP Status error [0x%X]", BSTD_FUNCTION, bspError ));
	    goto BHSM_P_DONE_LABEL;
	}


BHSM_P_DONE_LABEL:

	(void)BHSM_BspMsg_Destroy( hMsg );

	BDBG_LEAVE( BHSM_P_KeyLadder_RootConfig );

	return rc;
}


BERR_Code BHSM_P_KeyLadder_LayerSet( BHSM_Handle hHsm, BHSM_P_KeyLadderLayerSet *pParam )
{
	BERR_Code rc = BERR_SUCCESS;
	BHSM_BspMsg_h hMsg = NULL;
	uint16_t bspError = 0;
	BHSM_BspMsgCreate_t msgCreate;
	BHSM_BspMsgConfigure_t msgConfig;
	Bsp_KeyLadder_LayerSet_InFields_t *pSend = NULL;

	BDBG_ENTER( BHSM_P_KeyLadder_LayerSet );

	BKNI_Memset( &msgCreate, 0, sizeof(msgCreate) );
	BKNI_Memset( &msgConfig, 0, sizeof(msgConfig) );

	hMsg = BHSM_BspMsg_Create( hHsm, &msgCreate );
	if( !hMsg ) { return BERR_TRACE( BHSM_STATUS_RESOURCE_ALLOCATION_ERROR ); }

	pSend = (Bsp_KeyLadder_LayerSet_InFields_t*)msgCreate.pSend;

	msgConfig.component = Bsp_CmdComponent_eKeyLadder;
	msgConfig.command = Bsp_CmdKeyLadder_eLayerSet;

	rc = BHSM_BspMsg_Configure( hMsg, &msgConfig );
	if( rc != BERR_SUCCESS ) { return BERR_TRACE( rc ); }

	/* pack input paramters */
	pSend->vklId = pParam->in.vklId;
	pSend->keyLadderType = pParam->in.keyLadderType;
	pSend->destinationKeyLayer = pParam->in.destinationKeyLayer;
	pSend->keyLadderOperation = pParam->in.keyLadderOperation;
	BKNI_Memcpy( pSend->procIn, pParam->in.procIn, 32 );
	pSend->keySize = pParam->in.keySize;
	pSend->applyKkcv = pParam->in.applyKkcv;

	rc = BHSM_BspMsg_SubmitCommand ( hMsg, &bspError );
	if( rc != BERR_SUCCESS ) { BERR_TRACE( rc ); goto BHSM_P_DONE_LABEL; }

	if( bspError != 0 ) {
	    rc = BHSM_STATUS_BSP_ERROR;
	    BDBG_ERR(("%s BSP Status error [0x%X]", BSTD_FUNCTION, bspError ));
	    goto BHSM_P_DONE_LABEL;
	}


BHSM_P_DONE_LABEL:

	(void)BHSM_BspMsg_Destroy( hMsg );

	BDBG_LEAVE( BHSM_P_KeyLadder_LayerSet );

	return rc;
}


BERR_Code BHSM_P_KeyLadder_RouteIv( BHSM_Handle hHsm, BHSM_P_KeyLadderRouteIv *pParam )
{
	BERR_Code rc = BERR_SUCCESS;
	BHSM_BspMsg_h hMsg = NULL;
	uint16_t bspError = 0;
	BHSM_BspMsgCreate_t msgCreate;
	BHSM_BspMsgConfigure_t msgConfig;
	Bsp_KeyLadder_RouteIv_InFields_t *pSend = NULL;

	BDBG_ENTER( BHSM_P_KeyLadder_RouteIv );

	BKNI_Memset( &msgCreate, 0, sizeof(msgCreate) );
	BKNI_Memset( &msgConfig, 0, sizeof(msgConfig) );

	hMsg = BHSM_BspMsg_Create( hHsm, &msgCreate );
	if( !hMsg ) { return BERR_TRACE( BHSM_STATUS_RESOURCE_ALLOCATION_ERROR ); }

	pSend = (Bsp_KeyLadder_RouteIv_InFields_t*)msgCreate.pSend;

	msgConfig.component = Bsp_CmdComponent_eKeyLadder;
	msgConfig.command = Bsp_CmdKeyLadder_eRouteIv;

	rc = BHSM_BspMsg_Configure( hMsg, &msgConfig );
	if( rc != BERR_SUCCESS ) { return BERR_TRACE( rc ); }

	/* pack input paramters */
	pSend->blockType = pParam->in.blockType;
	pSend->entryType = pParam->in.entryType;
	pSend->keySlotType = pParam->in.keySlotType;
	pSend->keySlotNumber = pParam->in.keySlotNumber;
	pSend->vklId = pParam->in.vklId;
	pSend->keyLayer = pParam->in.keyLayer;
	pSend->ivType = pParam->in.ivType;

	rc = BHSM_BspMsg_SubmitCommand ( hMsg, &bspError );
	if( rc != BERR_SUCCESS ) { BERR_TRACE( rc ); goto BHSM_P_DONE_LABEL; }

	if( bspError != 0 ) {
	    rc = BHSM_STATUS_BSP_ERROR;
	    BDBG_ERR(("%s BSP Status error [0x%X]", BSTD_FUNCTION, bspError ));
	    goto BHSM_P_DONE_LABEL;
	}


BHSM_P_DONE_LABEL:

	(void)BHSM_BspMsg_Destroy( hMsg );

	BDBG_LEAVE( BHSM_P_KeyLadder_RouteIv );

	return rc;
}


BERR_Code BHSM_P_KeyLadder_FwklQuery( BHSM_Handle hHsm, BHSM_P_KeyLadderFwklQuery *pParam )
{
	BERR_Code rc = BERR_SUCCESS;
	BHSM_BspMsg_h hMsg = NULL;
	uint16_t bspError = 0;
	BHSM_BspMsgCreate_t msgCreate;
	BHSM_BspMsgConfigure_t msgConfig;
	Bsp_KeyLadder_FwklQuery_OutFields_t *pReceive = NULL;

	BDBG_ENTER( BHSM_P_KeyLadder_FwklQuery );

	BKNI_Memset( &msgCreate, 0, sizeof(msgCreate) );
	BKNI_Memset( &msgConfig, 0, sizeof(msgConfig) );

	hMsg = BHSM_BspMsg_Create( hHsm, &msgCreate );
	if( !hMsg ) { return BERR_TRACE( BHSM_STATUS_RESOURCE_ALLOCATION_ERROR ); }

	pReceive = (Bsp_KeyLadder_FwklQuery_OutFields_t*)msgCreate.pReceive;

	msgConfig.component = Bsp_CmdComponent_eKeyLadder;
	msgConfig.command = Bsp_CmdKeyLadder_eFwklQuery;

	rc = BHSM_BspMsg_Configure( hMsg, &msgConfig );
	if( rc != BERR_SUCCESS ) { return BERR_TRACE( rc ); }

	rc = BHSM_BspMsg_SubmitCommand ( hMsg, &bspError );
	if( rc != BERR_SUCCESS ) { BERR_TRACE( rc ); goto BHSM_P_DONE_LABEL; }

	if( bspError != 0 ) {
	    rc = BHSM_STATUS_BSP_ERROR;
	    BDBG_ERR(("%s BSP Status error [0x%X]", BSTD_FUNCTION, bspError ));
	    goto BHSM_P_DONE_LABEL;
	}

	/* extract output paramters */
	BKNI_Memcpy( pParam->out.fwklOwnership, pReceive->fwklOwnership, 8 );
	BKNI_Memcpy( pParam->out.fwklSubCustomerMode, pReceive->fwklSubCustomerMode, 8 );

BHSM_P_DONE_LABEL:

	(void)BHSM_BspMsg_Destroy( hMsg );

	BDBG_LEAVE( BHSM_P_KeyLadder_FwklQuery );

	return rc;
}


BERR_Code BHSM_P_KeyLadder_FwklInvalidate( BHSM_Handle hHsm, BHSM_P_KeyLadderFwklInvalidate *pParam )
{
	BERR_Code rc = BERR_SUCCESS;
	BHSM_BspMsg_h hMsg = NULL;
	uint16_t bspError = 0;
	BHSM_BspMsgCreate_t msgCreate;
	BHSM_BspMsgConfigure_t msgConfig;
	Bsp_KeyLadder_FwklInvalidate_InFields_t *pSend = NULL;

	BDBG_ENTER( BHSM_P_KeyLadder_FwklInvalidate );

	BKNI_Memset( &msgCreate, 0, sizeof(msgCreate) );
	BKNI_Memset( &msgConfig, 0, sizeof(msgConfig) );

	hMsg = BHSM_BspMsg_Create( hHsm, &msgCreate );
	if( !hMsg ) { return BERR_TRACE( BHSM_STATUS_RESOURCE_ALLOCATION_ERROR ); }

	pSend = (Bsp_KeyLadder_FwklInvalidate_InFields_t*)msgCreate.pSend;

	msgConfig.component = Bsp_CmdComponent_eKeyLadder;
	msgConfig.command = Bsp_CmdKeyLadder_eFwklInvalidate;

	rc = BHSM_BspMsg_Configure( hMsg, &msgConfig );
	if( rc != BERR_SUCCESS ) { return BERR_TRACE( rc ); }

	/* pack input paramters */
	pSend->keyLayer = pParam->in.keyLayer;
	pSend->vklId = pParam->in.vklId;
	pSend->clearAllKeyLayer = pParam->in.clearAllKeyLayer;
	pSend->freeOwnership = pParam->in.freeOwnership;

	rc = BHSM_BspMsg_SubmitCommand ( hMsg, &bspError );
	if( rc != BERR_SUCCESS ) { BERR_TRACE( rc ); goto BHSM_P_DONE_LABEL; }

	if( bspError != 0 ) {
	    rc = BHSM_STATUS_BSP_ERROR;
	    BDBG_ERR(("%s BSP Status error [0x%X]", BSTD_FUNCTION, bspError ));
	    goto BHSM_P_DONE_LABEL;
	}


BHSM_P_DONE_LABEL:

	(void)BHSM_BspMsg_Destroy( hMsg );

	BDBG_LEAVE( BHSM_P_KeyLadder_FwklInvalidate );

	return rc;
}


BERR_Code BHSM_P_KeyLadder_KladChallenge( BHSM_Handle hHsm, BHSM_P_KeyLadderKladChallenge *pParam )
{
	BERR_Code rc = BERR_SUCCESS;
	BHSM_BspMsg_h hMsg = NULL;
	uint16_t bspError = 0;
	BHSM_BspMsgCreate_t msgCreate;
	BHSM_BspMsgConfigure_t msgConfig;
	Bsp_KeyLadder_KladChallenge_InFields_t *pSend = NULL;
	Bsp_KeyLadder_KladChallenge_OutFields_t *pReceive = NULL;

	BDBG_ENTER( BHSM_P_KeyLadder_KladChallenge );

	BKNI_Memset( &msgCreate, 0, sizeof(msgCreate) );
	BKNI_Memset( &msgConfig, 0, sizeof(msgConfig) );

	hMsg = BHSM_BspMsg_Create( hHsm, &msgCreate );
	if( !hMsg ) { return BERR_TRACE( BHSM_STATUS_RESOURCE_ALLOCATION_ERROR ); }

	pSend = (Bsp_KeyLadder_KladChallenge_InFields_t*)msgCreate.pSend;
	pReceive = (Bsp_KeyLadder_KladChallenge_OutFields_t*)msgCreate.pReceive;

	msgConfig.component = Bsp_CmdComponent_eKeyLadder;
	msgConfig.command = Bsp_CmdKeyLadder_eKladChallenge;

	rc = BHSM_BspMsg_Configure( hMsg, &msgConfig );
	if( rc != BERR_SUCCESS ) { return BERR_TRACE( rc ); }

	/* pack input paramters */
	pSend->otpKeyId = pParam->in.otpKeyId;

	rc = BHSM_BspMsg_SubmitCommand ( hMsg, &bspError );
	if( rc != BERR_SUCCESS ) { BERR_TRACE( rc ); goto BHSM_P_DONE_LABEL; }

	if( bspError != 0 ) {
	    rc = BHSM_STATUS_BSP_ERROR;
	    BDBG_ERR(("%s BSP Status error [0x%X]", BSTD_FUNCTION, bspError ));
	    goto BHSM_P_DONE_LABEL;
	}

	/* extract output paramters */
	pParam->out.idHi = pReceive->idHi;
	pParam->out.idLo = pReceive->idLo;
	pParam->out.blackBoxId = pReceive->blackBoxId;
	pParam->out.stbOwnerId = pReceive->stbOwnerId;

BHSM_P_DONE_LABEL:

	(void)BHSM_BspMsg_Destroy( hMsg );

	BDBG_LEAVE( BHSM_P_KeyLadder_KladChallenge );

	return rc;
}


BERR_Code BHSM_P_KeyLadder_KladResponse( BHSM_Handle hHsm, BHSM_P_KeyLadderKladResponse *pParam )
{
	BERR_Code rc = BERR_SUCCESS;
	BHSM_BspMsg_h hMsg = NULL;
	uint16_t bspError = 0;
	BHSM_BspMsgCreate_t msgCreate;
	BHSM_BspMsgConfigure_t msgConfig;
	Bsp_KeyLadder_KladResponse_InFields_t *pSend = NULL;
	Bsp_KeyLadder_KladResponse_OutFields_t *pReceive = NULL;

	BDBG_ENTER( BHSM_P_KeyLadder_KladResponse );

	BKNI_Memset( &msgCreate, 0, sizeof(msgCreate) );
	BKNI_Memset( &msgConfig, 0, sizeof(msgConfig) );

	hMsg = BHSM_BspMsg_Create( hHsm, &msgCreate );
	if( !hMsg ) { return BERR_TRACE( BHSM_STATUS_RESOURCE_ALLOCATION_ERROR ); }

	pSend = (Bsp_KeyLadder_KladResponse_InFields_t*)msgCreate.pSend;
	pReceive = (Bsp_KeyLadder_KladResponse_OutFields_t*)msgCreate.pReceive;

	msgConfig.component = Bsp_CmdComponent_eKeyLadder;
	msgConfig.command = Bsp_CmdKeyLadder_eKladResponse;

	rc = BHSM_BspMsg_Configure( hMsg, &msgConfig );
	if( rc != BERR_SUCCESS ) { return BERR_TRACE( rc ); }

	/* pack input paramters */
	pSend->keyLayer = pParam->in.keyLayer;
	pSend->vklId = pParam->in.vklId;
	pSend->kladMode = pParam->in.kladMode;
	BKNI_Memcpy( pSend->nonce, pParam->in.nonce, 16 );

	rc = BHSM_BspMsg_SubmitCommand ( hMsg, &bspError );
	if( rc != BERR_SUCCESS ) { BERR_TRACE( rc ); goto BHSM_P_DONE_LABEL; }

	if( bspError != 0 ) {
	    rc = BHSM_STATUS_BSP_ERROR;
	    BDBG_ERR(("%s BSP Status error [0x%X]", BSTD_FUNCTION, bspError ));
	    goto BHSM_P_DONE_LABEL;
	}

	/* extract output paramters */
	BKNI_Memcpy( pParam->out.response, pReceive->response, 16 );

BHSM_P_DONE_LABEL:

	(void)BHSM_BspMsg_Destroy( hMsg );

	BDBG_LEAVE( BHSM_P_KeyLadder_KladResponse );

	return rc;
}


BERR_Code BHSM_P_KeyLadder_ConfigKkcv( BHSM_Handle hHsm, BHSM_P_KeyLadderConfigKkcv *pParam )
{
	BERR_Code rc = BERR_SUCCESS;
	BHSM_BspMsg_h hMsg = NULL;
	uint16_t bspError = 0;
	BHSM_BspMsgCreate_t msgCreate;
	BHSM_BspMsgConfigure_t msgConfig;
	Bsp_KeyLadder_ConfigKkcv_InFields_t *pSend = NULL;

	BDBG_ENTER( BHSM_P_KeyLadder_ConfigKkcv );

	BKNI_Memset( &msgCreate, 0, sizeof(msgCreate) );
	BKNI_Memset( &msgConfig, 0, sizeof(msgConfig) );

	hMsg = BHSM_BspMsg_Create( hHsm, &msgCreate );
	if( !hMsg ) { return BERR_TRACE( BHSM_STATUS_RESOURCE_ALLOCATION_ERROR ); }

	pSend = (Bsp_KeyLadder_ConfigKkcv_InFields_t*)msgCreate.pSend;

	msgConfig.component = Bsp_CmdComponent_eKeyLadder;
	msgConfig.command = Bsp_CmdKeyLadder_eConfigKkcv;

	rc = BHSM_BspMsg_Configure( hMsg, &msgConfig );
	if( rc != BERR_SUCCESS ) { return BERR_TRACE( rc ); }

	/* pack input paramters */
	pSend->keyLayer = pParam->in.keyLayer;
	pSend->vklId = pParam->in.vklId;
	pSend->operation = pParam->in.operation;
	BKNI_Memcpy( pSend->kkcv, pParam->in.kkcv, 16 );

	rc = BHSM_BspMsg_SubmitCommand ( hMsg, &bspError );
	if( rc != BERR_SUCCESS ) { BERR_TRACE( rc ); goto BHSM_P_DONE_LABEL; }

	if( bspError != 0 ) {
	    rc = BHSM_STATUS_BSP_ERROR;
	    BDBG_ERR(("%s BSP Status error [0x%X]", BSTD_FUNCTION, bspError ));
	    goto BHSM_P_DONE_LABEL;
	}


BHSM_P_DONE_LABEL:

	(void)BHSM_BspMsg_Destroy( hMsg );

	BDBG_LEAVE( BHSM_P_KeyLadder_ConfigKkcv );

	return rc;
}
